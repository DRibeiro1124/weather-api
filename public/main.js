const main = () => {
  // document.querySelector('h1').textContent += '?'
}

document.addEventListener('DOMContentLoaded', main)
document.querySelector('.weather-button').addEventListener('click', weatherButtonClickEvent)